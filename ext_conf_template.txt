# cat=basic/llm; type=int+; label=nr-llm Task UID: the nr-llm Task record whose LLM configuration (provider, model, system prompt) the chat runs under. Required — the chat stays unavailable while this is 0.
llmTaskUid = 0

# cat=basic/llm; type=int+; label=Maximum tool rounds per turn: how often the model may call tools and be asked again within one turn. Clamped by nr-llm's own ceiling.
maxIterations = 8

# cat=basic/access; type=string; label=Allowed backend user groups: comma-separated group UIDs. Empty means every backend user; administrators always have access.
allowedGroups =

# cat=basic/access; type=int+; label=Turns per minute and user: rate limit across all of a user's conversations. 0 disables the limit.
turnsPerMinute = 10

# cat=basic/access; type=int+; label=Maximum conversations per user (0 = unlimited)
maxConversationsPerUser = 50

# cat=basic/access; type=int+; label=Maximum concurrent runs per user: conversations that may be processing or awaiting approval at once (0 = unlimited)
maxActiveConversationsPerUser = 3

# cat=basic/security; type=int+; label=Maximum message length in characters (0 = unlimited)
maxMessageLength = 10000

# cat=basic/files; type=string; label=Attachment upload folder: FAL combined identifier below which chat attachments are stored, per backend user and conversation.
uploadFolder = 1:/ai_chat/

# cat=basic/retention; type=int+; label=Auto-archive conversations after days of inactivity (0 = never)
autoArchiveDays = 30

# cat=basic/retention; type=int+; label=Delete archived conversations, their messages and their attachments after days (0 = never)
attachmentRetentionDays = 90
